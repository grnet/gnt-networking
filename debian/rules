#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh $@

override_dh_auto_configure:
	./autogen.sh
	dh_auto_configure -- --libexecdir=/usr/lib

override_dh_install:
	dh_install
	cp $(CURDIR)/defaults $(CURDIR)/debian/gnt-networking.default
